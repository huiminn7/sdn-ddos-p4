# DDoS Detection with P4 + Mininet

## Quick Start

```bash
# 1. Install dependencies (first time only)
./scripts/install.sh

# 2. Compile P4 program
./scripts/compile.sh

# 3. Run the network
./scripts/run.sh

# 4. Clean up when done
./scripts/cleanup.sh
